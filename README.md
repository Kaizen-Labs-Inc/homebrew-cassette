# Homebrew Tap For Cassette

Install the Cassette CLI:

```bash
brew tap jordanful/cassette
brew install cassette
```

This tap contains the Homebrew formula for the `cassette` CLI. Release artifacts
and updated formulas are generated from the main Cassette application repo.
